<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pet">

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pet">
        <activity android:name=".mode.activities.AcceptFriendRequestActivity"></activity>
        <activity android:name=".mode.activities.ProfileActivity" />
        <activity android:name=".mode.activities.Update_Info_Activity" />
        <activity android:name=".mode.activities.UpdatePassActivity" />
        <activity android:name=".mode.activities.SearchFriendActivity">

        </activity>
        <activity android:name=".mode.activities.MessageActivity" />
        <activity android:name=".mode.activities.RegisterActivity2" />
        <activity
            android:name=".mode.activities.StatusActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".mode.activities.HomeActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".mode.activities.RegistrationActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".mode.activities.LoginActivity"
            android:theme="@style/Theme.Design.NoActionBar">

        </activity>
        <activity
            android:name=".mode.activities.MainPage"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".mode.activities.MainActivity"
            android:theme="@style/Theme.Design.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>